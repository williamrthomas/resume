---
import Layout from "../layouts/Layout.astro";
import Chatbot from "../components/Chatbot.tsx";
import JDMap from "../components/JDMap.tsx";
import { candidate } from "../data/content";
---

<Layout title="William R. Thomas — Forward Deployed Engineer Application">
  <!-- Top bar -->
  <header class="border-b border-ink-700/10 bg-cream-50/80 backdrop-blur sticky top-0 z-50">
    <div class="flex items-center justify-between px-6 py-3">
      <div class="flex items-center gap-3">
        <div class="h-8 w-8 rounded-md bg-ink-900 text-cream-50 flex items-center justify-center font-serif text-sm">W</div>
        <div class="leading-tight">
          <p class="text-sm font-medium text-ink-900">William R. Thomas</p>
          <p class="text-xs text-ink-500">FDE, Applied AI · Anthropic</p>
        </div>
      </div>
      <div class="flex items-center gap-2">
        <a href="/williamthomas-anthropic-fde-resume.html" target="_blank" rel="noopener" class="pill hover:bg-cream-200 transition">
          <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
            <polyline points="7 10 12 15 17 10"></polyline>
            <line x1="12" y1="15" x2="12" y2="3"></line>
          </svg>
          Résumé
        </a>
        <a href="/robotics-press-factory.png" target="_blank" rel="noopener" class="pill hover:bg-cream-200 transition">robotics.press ↗</a>
        <a href={candidate.links.github} target="_blank" rel="noopener" class="pill hover:bg-cream-200 transition hidden sm:inline-flex">GitHub ↗</a>
        <button id="logout-btn" class="text-xs text-ink-500 hover:text-ink-900 transition px-2 py-1">Sign out</button>
      </div>
    </div>
  </header>

  <JDMap client:load />

  <Chatbot client:load />

  <script is:inline>
    document.getElementById("logout-btn").addEventListener("click", async () => {
      await fetch("/api/logout", { method: "POST" });
      window.location.href = "/";
    });
  </script>
</Layout>
